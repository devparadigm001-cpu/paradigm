{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-badge",
  "description": "Capability for the floating Record Mode badge window",
  "windows": ["recording-badge"],
  "permissions": ["core:default"]
}
